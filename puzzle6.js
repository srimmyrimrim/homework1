right();
var color;
getColor();
color = getColor();
if (getColor() == "blue" )
	{
    			down();
    			down();
    			down();
    			setColor(color);
				right();
				down();
  }
			else
 	{
   				up();
    			up();
   				up();
   				setColor(color);
					left();
					up();
  }
